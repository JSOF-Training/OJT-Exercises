1. Read [here](https://linux-kernel-labs.github.io/refs/heads/master/labs/memory_mapping.html#struct-vm-area-struct).
2. Assume the `task_struct` to be some size, then try to think if you should already know the values of some fields in the `task_struct`. From there you can find the offsets of those fields.
